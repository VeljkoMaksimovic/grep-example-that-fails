version: v1.0
name: Semaphore Go CI example
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
blocks:
  - name: Build project
    task:
      jobs:
        - name: Grep command fails
          commands:
            - echo $TEST_ENV
      env_vars:
        - name: TEST_ENV
          value: TestPrivateLinkAwsE2E/TestPrivateAwsClientWorkflow
